export enum NotificationType {
  EVENT_APPROVED = 'event_approved',
  EVENT_REJECTED = 'event_rejected',
  ROLE_APPROVED = 'role_approved',
  ROLE_REJECTED = 'role_rejected',
  REGISTERED_EVENT_REMINDER = 'registered_event_reminder',
  FAVORITE_EVENT_REMINDER = 'favorite_event_reminder',
  REGISTERED_EVENT_UPDATED = 'registered_event_updated',
  FAVORITE_EVENT_UPDATED = 'favorite_event_updated',
  EVENT_DELETED = 'event_deleted',
}
