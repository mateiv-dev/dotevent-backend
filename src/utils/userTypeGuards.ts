// import { Role } from "@models/Role";
// import { Admin, Organizer, Student, StudentRep, UserDocument } from "@models/user";

// export function isStudent(user: UserDocument): user is UserDocument & Student {
//   return user.role === Role.student;
// }

// export function isStudentRep(user: UserDocument): user is UserDocument & StudentRep {
//   return user.role === Role.studentRep;
// }

// export function isOrganizer(user: UserDocument): user is UserDocument & Organizer {
//   return user.role === Role.organizer;
// }

// export function isAdmin(user: UserDocument): user is UserDocument & Admin {
//   return user.role === Role.admin;
// }
